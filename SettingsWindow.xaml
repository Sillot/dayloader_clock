<Window x:Class="DayloaderClock.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Paramètres — Dayloader Clock"
        Width="420" Height="480"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="Label">
            <Setter Property="Foreground" Value="#5A4228"/>
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#3A2E1E"/>
            <Setter Property="Foreground" Value="#E8D5B8"/>
            <Setter Property="BorderBrush" Value="#5A4830"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="CaretBrush" Value="#FFD700"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#5A4228"/>
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="FontFamily" Value="Consolas"/>
        </Style>
        <Style x:Key="WoodButton" TargetType="Button">
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="Padding" Value="18,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="7"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#5A4830"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Outer frame: dark base -->
    <Border CornerRadius="14" Margin="6" Background="#2C2419">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="16" ShadowDepth="4" Opacity="0.55"/>
        </Border.Effect>

        <!-- Inner panel: wood tone -->
        <Border CornerRadius="11" Margin="4"
                MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#D4B896" Offset="0.0"/>
                    <GradientStop Color="#C9AA80" Offset="0.3"/>
                    <GradientStop Color="#B8976A" Offset="0.7"/>
                    <GradientStop Color="#A8875A" Offset="1.0"/>
                </LinearGradientBrush>
            </Border.Background>

            <Grid Margin="24,14,24,18">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Title -->
                <TextBlock Grid.Row="0" Text="&#x2699;  PARAMÈTRES"
                           FontSize="14" FontWeight="Bold"
                           Foreground="#4A3520" FontFamily="Consolas"
                           HorizontalAlignment="Center" Margin="0,0,0,16"/>

                <!-- Work day hours -->
                <StackPanel Grid.Row="1" Margin="0,0,0,12">
                    <TextBlock Text="Durée journée de travail (heures)" Style="{StaticResource Label}"/>
                    <TextBox x:Name="txtWorkHours" Text="8"/>
                </StackPanel>

                <!-- Lunch start -->
                <StackPanel Grid.Row="2" Margin="0,0,0,12">
                    <TextBlock Text="Début pause déjeuner (HH:mm)" Style="{StaticResource Label}"/>
                    <TextBox x:Name="txtLunchStart" Text="12:00"/>
                </StackPanel>

                <!-- Lunch duration -->
                <StackPanel Grid.Row="3" Margin="0,0,0,12">
                    <TextBlock Text="Durée pause déjeuner (minutes)" Style="{StaticResource Label}"/>
                    <TextBox x:Name="txtLunchDuration" Text="60"/>
                </StackPanel>

                <!-- Auto-start -->
                <CheckBox Grid.Row="4" x:Name="chkAutoStart"
                          Content="  Démarrer automatiquement avec Windows"
                          Margin="0,4,0,0" IsChecked="True"/>

                <!-- Pomodoro duration -->
                <StackPanel Grid.Row="5" Margin="0,12,0,12">
                    <TextBlock Text="Durée Pomodoro (minutes)" Style="{StaticResource Label}"/>
                    <TextBox x:Name="txtPomodoro" Text="25"/>
                </StackPanel>

                <!-- Pomodoro DND -->
                <CheckBox Grid.Row="6" x:Name="chkPomodoroDnd"
                          Content="  Activer Ne Pas Déranger pendant Pomodoro"
                          Margin="0,0,0,0" IsChecked="True"/>

                <!-- Version -->
                <TextBlock Grid.Row="7" Text="v1.0.0" x:Name="txtVersion"
                           Foreground="#8A7560" FontSize="10" FontFamily="Consolas"
                           HorizontalAlignment="Left" VerticalAlignment="Center"
                           Opacity="0.6" Margin="0,4,0,0"/>

                <!-- Buttons -->
                <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Annuler"
                            Style="{StaticResource WoodButton}"
                            Background="#3A2E1E" Foreground="#A89070"
                            BorderBrush="#5A4830" Margin="0,0,10,0"
                            Click="Cancel_Click"/>
                    <Button Content="Enregistrer"
                            Style="{StaticResource WoodButton}"
                            Background="#4A3520" Foreground="#FFD700"
                            BorderBrush="#6B5035" FontWeight="Bold"
                            Click="Save_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Border>
</Window>
