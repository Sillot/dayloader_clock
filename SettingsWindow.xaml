<Window x:Class="DayloaderClock.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:res="clr-namespace:DayloaderClock.Resources"
        Title="{x:Static res:Strings.Settings_Title}"
        Width="420" Height="560"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="Label">
            <Setter Property="Foreground" Value="#5A4228"/>
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#3A2E1E"/>
            <Setter Property="Foreground" Value="#E8D5B8"/>
            <Setter Property="BorderBrush" Value="#5A4830"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="CaretBrush" Value="#FFD700"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#5A4228"/>
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Border x:Name="box" Width="20" Height="20"
                                    Background="#3A2E1E" BorderBrush="#5A4830"
                                    BorderThickness="1" CornerRadius="4"
                                    VerticalAlignment="Center">
                                <Path x:Name="check" Data="M 3 8 L 7 12 L 14 4"
                                      Stroke="#FFD700" StrokeThickness="2"
                                      Margin="1,0,0,0"
                                      Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter Margin="6,0,0,0" VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="check" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="box" Property="BorderBrush" Value="#8A7560"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- ComboBox styling -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#3A2E1E"/>
            <Setter Property="Foreground" Value="#E8D5B8"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#5A4830"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Border" CornerRadius="6"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="#5A4830" BorderThickness="1">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition/>
                                                    <ColumnDefinition Width="28"/>
                                                </Grid.ColumnDefinitions>
                                                <Path Grid.Column="1" Fill="#A89070" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                      Data="M 0 0 L 5 5 L 10 0 Z"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#4A3A28"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                                <ToggleButton.Background>
                                    <SolidColorBrush Color="#3A2E1E"/>
                                </ToggleButton.Background>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              Margin="12,8,28,8"
                                              VerticalAlignment="Center" HorizontalAlignment="Left"/>
                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid x:Name="DropDown" SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}" MaxHeight="300">
                                    <Border x:Name="DropDownBorder" CornerRadius="6" Margin="0,2,0,0"
                                            Background="#3A2E1E" BorderBrush="#5A4830" BorderThickness="1">
                                        <ScrollViewer Margin="4,6" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="#E8D5B8"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" CornerRadius="4" Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#5A4830"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#4A3A28"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="WoodButton" TargetType="Button">
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="Padding" Value="18,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="7"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#5A4830"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Outer frame: dark base -->
    <Border CornerRadius="14" Margin="6" Background="#2C2419">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="16" ShadowDepth="4" Opacity="0.55"/>
        </Border.Effect>

        <!-- Inner panel: wood tone -->
        <Border CornerRadius="11" Margin="4"
                MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#D4B896" Offset="0.0"/>
                    <GradientStop Color="#C9AA80" Offset="0.3"/>
                    <GradientStop Color="#B8976A" Offset="0.7"/>
                    <GradientStop Color="#A8875A" Offset="1.0"/>
                </LinearGradientBrush>
            </Border.Background>

            <Grid Margin="24,14,24,18">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Title -->
                <TextBlock Grid.Row="0" Text="{x:Static res:Strings.Settings_Header}"
                           FontSize="14" FontWeight="Bold"
                           Foreground="#4A3520" FontFamily="Consolas"
                           HorizontalAlignment="Center" Margin="0,0,0,16"/>

                <!-- Work day hours -->
                <StackPanel Grid.Row="1" Margin="0,0,0,12">
                    <TextBlock Text="{x:Static res:Strings.Settings_WorkDayHours}" Style="{StaticResource Label}"/>
                    <TextBox x:Name="txtWorkHours" Text="8"/>
                </StackPanel>

                <!-- Lunch start -->
                <StackPanel Grid.Row="2" Margin="0,0,0,12">
                    <TextBlock Text="{x:Static res:Strings.Settings_LunchStart}" Style="{StaticResource Label}"/>
                    <TextBox x:Name="txtLunchStart" Text="12:00"/>
                </StackPanel>

                <!-- Lunch duration -->
                <StackPanel Grid.Row="3" Margin="0,0,0,12">
                    <TextBlock Text="{x:Static res:Strings.Settings_LunchDuration}" Style="{StaticResource Label}"/>
                    <TextBox x:Name="txtLunchDuration" Text="60"/>
                </StackPanel>

                <!-- Auto-start -->
                <CheckBox Grid.Row="4" x:Name="chkAutoStart"
                          Content="{x:Static res:Strings.Settings_AutoStart}"
                          Margin="0,4,0,0" IsChecked="True"/>

                <!-- Language -->
                <StackPanel Grid.Row="5" Margin="0,12,0,0">
                    <TextBlock Text="{x:Static res:Strings.Settings_Language}" Style="{StaticResource Label}"/>
                    <ComboBox x:Name="cmbLanguage"/>
                </StackPanel>

                <!-- Version -->
                <TextBlock Grid.Row="6" Text="v1.0.0" x:Name="txtVersion"
                           Foreground="#8A7560" FontSize="10" FontFamily="Consolas"
                           HorizontalAlignment="Left" VerticalAlignment="Center"
                           Opacity="0.6" Margin="0,4,0,0"/>

                <!-- Buttons -->
                <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="{x:Static res:Strings.Settings_Cancel}"
                            Style="{StaticResource WoodButton}"
                            Background="#3A2E1E" Foreground="#A89070"
                            BorderBrush="#5A4830" Margin="0,0,10,0"
                            Click="Cancel_Click"/>
                    <Button Content="{x:Static res:Strings.Settings_Save}"
                            Style="{StaticResource WoodButton}"
                            Background="#4A3520" Foreground="#FFD700"
                            BorderBrush="#6B5035" FontWeight="Bold"
                            Click="Save_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Border>
</Window>
